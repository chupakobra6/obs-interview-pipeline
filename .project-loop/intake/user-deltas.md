# Пользовательские Дельты

Проект: obs-interview-pipeline
Обновлено: 2026-08-02

Этот файл хранит существенные свежие корректировки, решения и изменения области от пользователя, которые приходят во время шага. Записи компактны и пополняются до отражения в источнике правды и handoff.

## Записи

### Качественный long-form ASR без искусственных границ

ID источника: `S009`

Исходный ввод:

```text
смотри правильно тут сказано я не хочу терять качество подумай над всем этим и реализуй качественное решение для этого вопроса правильно умное архитектурно четко и правильно по принципам и коду если надо поищи опыт в интернете надо хорошее решение стройное и чтобы нормально было вписано в то что щас есть
```

Нормализация:
- [x] качество и полнота long-form transcript важнее сохранения текущей фиксированной схемы чанков;
- [x] устранить искусственные разрывы каждые 120 секунд и word-overlap merge как основной алгоритм;
- [x] сначала проверить нативный long-form decode Whisper с timestamps после bounded leading-silence trim; внешний pause-aware fallback вводить только при подтверждённой необходимости;
- [x] сохранить единственный Harvest runtime/profile и opt-in границу OBS, не меняя Telegram `daily`, `daily-catchup` и обычный `transcribe-file`;
- [x] подтвердить решение regression-набором и A/B-прогоном реального собеседования, включая начало, середину, конец и прежние границы.

Маршрутизация:
- [x] исходный ввод записан здесь
- [x] карта источников обновлена
- [x] чеклист обновлен
- [x] план и текущий шаг обновлены
- [x] реализация завершена
- [x] reviewer проверил
- [x] handoff обновлен

### Канонический ASR Telegram Harvest и кликабельное уведомление

ID источника: `S003`

Исходный ввод:

```text
сделай так чтобы движок виспера использовался именно тот что в телеграм харвест - чтобы мне не приходилось апдейтить в двух местах, логику да можешь отдельно сдедлать

проект оформи как соседние проекты, гит заведи если нет

также при нажатии на увед на макос пусть кидает в папку с собесом
```

Нормализация:
- [x] требование: OBS pipeline вызывает канонический production ASR Telegram Harvest и не хранит собственную Whisper-реализацию или профиль.
- [x] требование: клик по success-уведомлению macOS открывает каталог готового собеседования.
- [x] требование: репозиторий оформлен в стиле соседних Go-проектов и находится под Git.

Маршрутизация:
- [x] исходный ввод сохранен или записан здесь
- [x] карта источников обновлена
- [x] чеклист обновлен
- [x] план и текущий шаг обновлены
- [x] реализация направлена в рабочий шаг или отложена
- [x] reviewer проверил
- [x] handoff обновлен

### Диалог обработки, сохранение дорожек и корректное начало ASR

ID источника: `S008`

Исходный ввод:

```text
почему-то расшифровка настоящего видео некорректная - начало обрезано и галлюцинации в начале, то есть звук норм на видео но на расшифровке не с начала текст идет

дорожки давай сохранять короче как есть просто качество снизим

а можем мы какое-то диалоговое окно сделать чтобы при завершении записи оно спрашивало - типо сжать и бахнуть и там галочка была удалить сурс не удалить и типо смержить дорожки или нет?
```

Нормализация:
- [x] диагностировать начало ASR на сохранённом реальном собеседовании и устранить подтверждённую причину без возврата медленного whole-file gate;
- [x] после OBS Stop неблокирующее native macOS-окно предлагает обработать либо оставить запись;
- [x] флажок удаления source задаёт delete policy конкретного job;
- [x] по умолчанию все исходные audio tracks сохраняются раздельно и перекодируются в AAC 96 Кбит/с;
- [x] отдельный флажок позволяет свести tracks в одну дорожку; без него число дорожек сохраняется;
- [x] отказ от обработки не ставит job в очередь и оставляет source без изменений.

Маршрутизация:
- [x] исходный ввод записан здесь
- [x] карта источников обновлена
- [x] чеклист обновлен
- [x] план и текущий шаг обновлены
- [x] реализация и UI-проверка завершены
- [x] reviewer проверил
- [x] handoff обновлен

Конфликты:
- Новая инструкция отменяет прежнее ограничение `CON-002` не изменять `telegram-harvest`: разрешено добавить локальный read-only ASR-вход и общий контракт.

### Временный notification с отложенным кликом

ID источника: `S004`

Исходный ввод:

```text
который остаётся жив до клика

вот это можно вернуть временно чтобы было потому что я потом панель открою и увижу его все равно, оно не фул пропадает
```

Нормализация:
- [x] стиль `OBS Interview Notifier` в macOS — «Временно»;
- [x] невидимый helper не завершается после исчезновения баннера и ждёт клика;
- [x] карточка остаётся в Центре уведомлений и открывает точный result directory позже.

Маршрутизация:
- [x] исходный ввод сохранен здесь
- [x] карта источников обновлена
- [x] чеклист обновлен
- [x] реализация и UI-проверка завершены
- [x] reviewer проверил
- [x] handoff обновлен

### Реальное собеседование через pipeline

ID источника: `S005`

Исходный ввод:

```text
также проведи через пайплайн там ласт видео в фильмах папке это собес
```

Нормализация и результат:
- [x] выбран только последний video-файл верхнего уровня `~/Movies`: `2026-07-31 13-28-17.mp4`;
- [x] job завершён без ошибки через установленный LaunchAgent/Harvest/VideoToolbox pipeline;
- [x] HEVC `1512x982@30`, три AAC, transcript и manifest проверены;
- [x] исходник удалён только после публикации и проверки по явному разрешению пользователя.

### Финальный tooling-review и cleanup

ID источника: `S006`

Исходный ввод:

```text
проверь что не оставил за собой никакого мусора все круто четко тулинг ревью репо полишь
```

Нормализация и результат:
- [x] выполнены `tooling-review` и `repo-polish`;
- [x] disposable output и тестовые job records перемещены в Корзину;
- [x] runtime-state оставляет только реальное собеседование;
- [x] native notifier добавлен в `make check` и macOS CI;
- [x] helper завершает работу не только после клика, но и после очистки карточки;
- [x] notifier launch errors попадают в processor error log;
- [x] reviewer проверил и handoff обновлен.

### Быстрый OBS-профиль без whole-file gate и повторного video encode

ID источника: `S007`

Исходный ввод:

```text
Для OBS-собеседований пропускать whole-file Silero gate, оставив тот же Whisper large-v3-turbo-q5_0, Metal, русский профиль и post-filter из Harvest.

Если вход уже 1512×982@30, не запускать повторно scale=lanczos,fps=30.

Максимальное ускорение — записывать OBS сразу в HEVC через Apple VideoToolbox.

оставлять только одну мастер дорожку, первую; AAC 96 Кбит/с — норм.
```

Нормализация:
- [x] OBS вызывает явный локальный режим Harvest, который пропускает только whole-file Silero gate и сохраняет канонические model/decode/Metal/language/post-filter настройки.
- [x] готовый HEVC 1512x982@30 копируется без video re-encode; scale/fps добавляются только при фактическом несовпадении входа.
- [x] OBS пишет HEVC через Apple VideoToolbox, сохраняя три исходные дорожки до успешной обработки.
- [x] финальный MP4 содержит первую master-дорожку AAC 96 Кбит/с; исходник удаляется прежним безопасным delete gate.
- [x] короткий current-head OBS E2E подтверждает весь контракт и после проверки не оставляет disposable artifacts.

Маршрутизация:
- [x] исходный ввод записан здесь
- [x] карта источников обновлена
- [x] чеклист обновлен
- [x] план и текущий шаг обновлены
- [x] реализация и UI-настройка завершены
- [x] reviewer проверил
- [x] handoff обновлен
